import React from 'react';
import { useDispatch,useSelector } from 'react-redux';

export default function SinglePoll(props){
    const dispatch=useDispatch();
    const getId= useSelector((state)=>state);
    return(
        <div>
        </div>
    )
}